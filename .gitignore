node_modules
dist
.accio
.accio-site.json
.npmrc
.wrangler
# Only package-lock.json ships. The pnpm lockfile is local tooling state and a
# stale lockfileVersion would break the Cloudflare Pages builder.
pnpm-lock.yaml
pnpm-workspace.yaml
.env
.env.local
.env.*.local
.dev.vars
.DS_Store
*.sqlite
*.sqlite3
*.parquet
dump/
*.log
